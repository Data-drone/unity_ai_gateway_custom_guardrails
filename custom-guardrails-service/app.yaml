command:
  - "uvicorn"
  - "app:app"
  - "--host"
  - "0.0.0.0"
  - "--port"
  - "8000"

env:
  # Required when registering this App as a CUSTOM model provider.
  # Store the value in a Databricks secret and wire it via the Apps UI / resources.
  - name: JUDGE_API_KEY
    value: ""
